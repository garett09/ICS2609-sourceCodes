/* eslint-disable linebreak-style */
// Custom module
const pi = 3.1416;

const add = (x, y) => x+y;
const subtract = (x, y) => x-y;
const multiply = (x, y) => x*y;
const divide = (x, y) => x/y;

module.exports = {
  pi: pi,
  add: add,
  subtract: subtract,
  multiply: multiply,
  divide: divide,
};

